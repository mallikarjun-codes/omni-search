{
  "name": "omni-search",
  "version": "1.0.0",
  "description": "Root orchestration for Omni Search RAG Bot",
  "scripts": {
    "start-backend": "npm start --prefix backend",
    "start-frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run start-backend\" \"npm run start-frontend\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
